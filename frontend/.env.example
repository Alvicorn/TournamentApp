# Copy to .env and fill in. Baked into the build by Vite.
# For production, use the Render backend service URL (https://<service-name>.onrender.com)

VITE_API_URL=http://localhost:8000
VITE_VAPID_PUBLIC_KEY=
